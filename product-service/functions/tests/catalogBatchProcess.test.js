describe("catalogBatchProcess function", () => {
  it.todo("should store every product from 'Records' property");

  it.todo("should send SNS message after successfully adding products");

  it.todo(
    "should send SNS message with company = LEGO if any of the product is LEGO"
  );

  it.todo(
    "should send SNS message with company = company of first product, if no LEGO was added"
  );
});
